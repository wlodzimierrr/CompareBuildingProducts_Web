/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['media.screwfix.com', 'kingfisher.scene7.com'], 
    },
    reactStrictMode: true,
    swcMinify: true,

  };
  
  export default nextConfig;
  